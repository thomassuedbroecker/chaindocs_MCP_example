Architecture notes:

- FastMCP exposes the tools over stdio.
- LangChain text loaders read local files.
- RecursiveCharacterTextSplitter creates searchable chunks.
- The example uses lightweight keyword ranking instead of embeddings.
