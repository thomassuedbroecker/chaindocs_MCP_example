# Getting Started

This example MCP server indexes local Markdown and text files.

Use `search_documents` to find matching chunks and `read_document` to read the full source file.

The default setup points at the `sample_documents` folder so the server has data immediately after installation.
